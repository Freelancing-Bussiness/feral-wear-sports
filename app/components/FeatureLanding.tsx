import StoreChrome from "./StoreChrome";import ComingSoon from "./ComingSoon";
export type FeatureItem={number:string,title:string,text:string,href?:string};
export default function FeatureLanding({eyebrow,title,description,items,accent="acid"}:{eyebrow:string;title:string;description:string;items:FeatureItem[];accent?:"acid"|"dark"}){return <StoreChrome><main className={`feature-landing ${accent}`}><header><p>{eyebrow}</p><h1>{title}</h1><span>{description}</span></header><ComingSoon context="products and programs"/><section>{items.map(item=><article key={item.number}><b>{item.number}</b><h2>{item.title}</h2><p>{item.text}</p>{item.href&&<a href={item.href}>EXPLORE →</a>}</article>)}</section><aside><strong>FERAL WEAR</strong><span>Prototype information will be replaced with approved commercial, nutritional and promotional terms before launch.</span></aside></main></StoreChrome>}
