import StoreChrome from "./StoreChrome";import ComingSoon from "./ComingSoon";import {CommerceSection} from "../commerce-navigation";
export default function CategoryLanding({section,focus}:{section:CommerceSection;focus?:string}){return <StoreChrome><main className="department-page"><header className="department-hero" style={{backgroundImage:`linear-gradient(90deg,rgba(0,0,0,.86),rgba(0,0,0,.1)),url(${section.hero})`}}><p>FERAL / {section.label}</p><h1>{focus??section.label}</h1><span>{section.tagline}</span></header><section className="department-links">{section.groups.map(group=><div key={group.title}><p>{group.title}</p>{group.items.map(item=><a className={focus===item.label?"active":""} href={`/collections/${item.slug}`} key={item.slug}>{item.label}<span>↗</span></a>)}</div>)}</section><ComingSoon context={`${focus??section.label} products`}/></main></StoreChrome>}
