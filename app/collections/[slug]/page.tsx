import CategoryLanding from "../../components/CategoryLanding";import {subcategoryBySlug} from "../../commerce-navigation";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=subcategoryBySlug(slug);return {title:item?`${item.label} | FERAL Wear`:"Collection | FERAL Wear",description:item?`Shop ${item.label.toLowerCase()} in the FERAL ${item.section.label} collection.`:"Explore FERAL Wear collections."}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=subcategoryBySlug(slug);if(!item)return <main style={{padding:80}}><h1>Collection not found</h1><a href="/shop">Back to shop</a></main>;return <CategoryLanding section={item.section} focus={item.label}/>}
